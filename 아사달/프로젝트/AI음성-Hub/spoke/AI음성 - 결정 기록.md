---
tags:
  - 아사달
  - 프로젝트
  - AI음성
  - 결정기록
created: 2026-08-21
updated: 2026-09-04
---

# AI음성 - 결정 기록

> [!summary] 한 줄 요약
> AI음성에서 확정된 설계·운영 결정과 **기각한 대안**의 모음. "왜 이렇게 했더라 / 왜 안 했더라"를 다시 조사하지 않기 위한 노트다. 결정이 뒤집히면 해당 행을 갱신한다. 서비스: [[AI음성]]

## 네트워크·인프라

| 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|
| **TURN 미도입** | 모바일 무반응의 원인이 프론트 0-후보 오퍼였고, ICE 수집 대기 패치로 해결됨 | TURN 서버 도입 | [[AI음성 - 모바일 ICE 원인 확정]] |
| **외부발 신규 인바운드 UDP 방화벽 허용 안 함** | 프론트 패치로 해결돼 인프라 변경 불필요 | 방화벽 정책 변경 | [[AI음성 - 모바일 정상 동작 확인]] |
| **서버 STUN 기본 제거** (`iceServers=[]`) | 서버는 공인 IP라 srflx == host. STUN 대기가 세션당 5초 지연의 원인 | 기본 STUN 유지 (`ENABLE_SERVER_STUN=on` 으로 복귀 가능) | [[AI음성 - 서버 ICE 수집 5초 지연 제거]] |
| **`gunicorn.conf.py` 의 `worker=` 오타 그대로 두기** | 명령줄 인자가 우선해 무해. 고치다 인자가 빠지면 워커 1개가 되는 위험이 더 큼 | 오타 수정 | [[AI음성 - 원본 브랜치 동등화와 프론트 기록 최신화]] |

## 브랜치·배포

| 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|
| **main PR 은 원본(main 기준) 브랜치로** — dev 테스트는 `-dev` 사본 | dev 소스가 main 에 유입되면 안 됨 (팀 규칙) | dev 사본으로 main PR | [[음성채팅 v4 복원]] |
| **v4 기본 활성** (`ENABLE_VOICE_V4` 기본 on) | 배포 즉시 경로가 열리고, 문제 시 코드 배포 없이 env 로 끔 | 기본 꺼짐 + 수동 활성 | [[AI음성 - v4 기본 활성 전환과 테스트 경로 정리]] |
| **dev PR 브랜치는 dev 가 아직 안 받은 main 커밋을 끌고 가지 않는다** — 그런 커밋이 있으면 `git merge-base origin/main origin/dev` 기준으로 브랜치를 딴다(또는 리베이스) | dev·main 의 `docker-compose.yml` 은 컨테이너명·포트(15252/5252)가 일부러 다르다. main 최신에서 딴 브랜치가 팀장 배포 커밋 4개를 dev PR 에 실어 compose 충돌(2026-09-03) | dev 를 브랜치로 풀 머지 / PR 에서 수동 충돌 해결 | [[AI음성 - 일시정지·멈춤 재생 제어 구현]] · 근본 해결은 compose 값의 `.env` 치환(팀장 제안 대상) |
| **`docker-compose.yml` 은 git 추적 제외** — `docker-compose*.yml` gitignore, 참조본은 `docs/DEPLOY.md` 코드블록, 서버 로컬 관리 (팀장 결정, 2026-09-03) | dev·운영 compose 가 달라야 하는데 추적하면 브랜치마다 충돌. 배포 워크플로의 `reset --hard` 는 추적 파일만 되돌리므로 서버 파일은 유지됨 | `.env` 치환(`${VOICE_PORT:-5252}`)으로 한 파일 유지 — 구조 변경이 git 으로 흐르는 장점이 있었으나 미채택 / 예시 파일 추적 — 머지 시 compose 관련 파일이 생기지 않게 해달라는 요청으로 제거 | [[AI음성 - compose 추적 제외와 브랜치 정리]] |
| **위 "merge-base 기준 브랜치" 규칙은 compose 추적 제외로 해소** — 다시 main 최신에서 따도 된다 | 충돌 원인 파일이 저장소에서 사라짐 | — | 〃 |

## 아키텍처

| 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|
| **v3 불가침** — v3 소스 바이트 무변경, 스모크 테스트로 고정 | 2026-07-13 롤백의 근본 원인(v3 하이재킹) 재발 방지 | v3 위에 v4 수정 얹기 | [[v4 복원 작업 현황]] |
| **v3·v4 orchestrator 세션별 분기 + v4 패키지 자립(사본)** | v4 를 고치려고 v3 를 건드리는 구조 자체를 제거 | 공용 `server_webrtc` 경유 (복원 초기 상태) | [[AI음성 - v3·v4 orchestrator 분리]] |
| **GA 판정을 접두사 규칙(`gpt-realtime-2*`)으로 통일** | 고정 집합 판정은 2.1 을 폐지된 Legacy 경로로 조용히 떨어뜨림. 프론트와 동일 규칙 | 고정 집합 `{2, 2.0}` 판정 | [[AI음성 - Realtime 모델 2.1 전환과 GA 판정 통일]] |
| **자동 응답 복원 + v4 GA 세션 한정 LangGraph 스킵** | `create_response=False` 차단 방식은 느린 서버 경로(전사 대기+턴 버퍼+Decision LLM)만 남김 — 오리지널 방향 복귀 | 자동 응답 차단 유지 | [[AI음성 - v4 응답 지연 원인 규명과 GA 즉답 경로 복원]] |
| **Tool-Only 휴면 분기 제거** (v2 opt-in 계약째 삭제) | 세팅처 0건 확인 — 죽은 유연성 | 분기 유지 | [[AI음성 - Tool-Only 휴면 분기 제거]] |
| **세션 요청 기본 모델을 GA(`gpt-realtime-2.1`)로** | 기존 기본값 `gpt-realtime-1.5` 는 2026-05-12 폐지된 Beta 평면 스키마 경로 — 프론트가 `model` 을 생략하면 죽은 API 로 세션을 열러 갔다 | v4 만 바꾸고 v3 는 존치 | [[AI음성 - 음성선택 기획서 백엔드 3건]] |
| **voice 를 기획서 10종 `Literal` 로 검증** | 오타가 그대로 OpenAI 로 가면 세션 생성 실패 = 통화 자체가 안 열린다. 라우터에서 422 로 선차단 | 검증 없이 통과시키기 / 별도 음성 목록 API (OpenAPI 스키마가 이미 목록이다) | [[음성 선택과 대기시간 옵션]] |
| **대기시간 미사용은 게이트 없는 `UplinkAudioProxyTrackV4`** | 기반 클래스 직접 사용은 `max_buffer_ms` 가 320 으로 되돌아가 모바일 지터 대응(960) 무효화 + 업링크가 `PROXY-DOWN` 으로 찍혀 방향 구분 소실 | v3 `AudioProxyTrack` 직접 사용 / 게이트 내부 플래그 분기 | [[음성 선택과 대기시간 옵션]] |
| **GA 판정을 계열 전체(`gpt-realtime` 접두사)로 확장 + legacy 분기 4종 제거** | 실검증(2026-08-21): GA 엔드포인트가 1.5 포함 전 계열을 서빙(각 201), Beta 경로는 모델 무관 400. 기획 V1.1 이 1.5 를 선택지로 확정 | 기획에서 1.5 제외 요청 / legacy 분기 존치(성공 불가능한 죽은 코드) | [[AI음성 - 1.5 GA 실검증과 legacy 경로 제거]] |
| **기획서 "2.0" 표기의 실제 모델 ID 는 `gpt-realtime-2`** | `gpt-realtime-2.0` 은 404 model_not_found (실검증) | — | [[음성 선택과 대기시간 옵션]] |
| **RAG 0건 시 미확인 안내 지시** — 자체 지식 답변 유도 폐기 (2026-08-28) | 학습 데이터에 없는 구청장 이름을 "근거에 따르면"이라며 단정하는 오답의 원인. 침묵 방지 목적은 "찾지 못했다고 반드시 말하라" 지시로 유지 | 자체 지식 답변 유도 존치 / answer 빈 문자열 복귀(침묵 재발) | [[AI음성 - RAG 0건 폴백 미확인 안내 전환]] |
| **안내문 사용 여부는 첫·종료 각각 컬럼 2개** (`use_voice_first_msg`·`use_voice_end_msg`) | 기획서 v2.0.4 시안에 라디오가 두 메시지에 각각 그려져 있음. 나중에 합치기보다 나누기가 쉬움 | 플래그 1개로 두 문구 동시 제어 | [[음성채팅 안내문 온오프와 재생 제어]] |
| **미사용 판정은 로더에서 빈 문자열 반환** — 라우터·브릿지 무수정 | v4 라우터가 이미 `if greeting:` 으로 metadata 를 조건 주입하고, 종료말 부재 시 `voice_end.done` 즉시 응답 경로가 있음. 빈 문자열 하나로 발화·말풍선·종료 흐름이 모두 정합 | 라우터에 플래그 분기 추가 / 브릿지에 스킵 상태 추가 | 〃 |
| **일시정지는 서버 다운링크 지터버퍼 버퍼링(진짜 일시정지), v4 전용** | OpenAI Realtime·WebRTC 에 일시정지 없음. 일시정지 중 상한 960ms → 3분으로 재개 잘림 제거. 재생 완료는 서버 `voice_playback.done`(버퍼 소진 + 100ms 디바운스)으로 통지 — OpenAI `stopped` 는 일시정지 중에도 온다. 재개 직후 백로그는 상한 아래로 내려갈 때까지 폐기하지 않음 | 스피커 mute 만(중간 유실) / 브라우저 Web Audio 버퍼링(v3 포함, 규모 큼) | 〃 (`feat/voice-playback-control`) |
| **종료말 완료(`voice_end.done`)도 서버 버퍼 드레인 뒤에** | OpenAI `stopped` 직후 보내면 서버 잔여분이 잘림. 프론트 800ms 유예에 기대던 것을 서버가 정확히 맞춤 | `stopped` 즉시 전송 유지 | 〃 |
| **끼어들기·멈춤·종료 시 서버 다운링크 버퍼도 비움** | OpenAI 는 자기 버퍼만 비워 서버 꼬리 최대 ~1초가 이어서 들렸음(일시정지 중이면 전체) | 꼬리 방치 | 〃 |
| **미리듣기는 Realtime WebSocket 1회 합성 — 저장 없음, 요청마다 합성** (2026-09-04, TTS 판을 대체) | 통화 인사말은 Realtime 이 직접 읽으므로 미리듣기도 같은 모델·목소리여야 "들은 것 = 들을 것". TTS(`gpt-4o-mini-tts`)는 같은 cedar 이름을 받아도 **청취 비교에서 목소리 차이 확인**. 지연은 실측 TTS 1.6~3.8초 vs Realtime 1.7~5.0초로 1초 안팎 차이 | TTS 단발 합성(구현 완료 상태에서 대체) / 저장 시 1회 녹음 파일 재생(회장님 제안) — 사용자 페이지가 이미 TTS 를 안 써 절약분 없음, 목소리 차이를 실제 통화 첫 문장으로 옮김, 미저장 문구 청취 불가, 본체 저장 흐름·인사말 경로 침범 / 문구 해시 캐시 — 같은 문구 반복 청취가 잦을 때만 / 스트리밍 응답 — 편차가 연결·응답 시작 구간이라 이득 없음, 프론트 Web Audio 필요 | [[AI음성 - 미리듣기 Realtime 1회 합성 전환]] |
| **미리듣기 세션은 최소 설정(목소리·출력 포맷)** — 통화용 시스템 프롬프트·도구 미주입 | 도구는 실행기가 없고 RAG 규칙은 응답을 흔든다. 말투 지시는 들어 보고 다르면 추가 | 통화용 `_build_session_ga` 결과에서 도구만 제거해 사용 | 〃 |
| **미리듣기 벤더 분기는 `speak_once(text, model, voice, api_key)` 시그니처 + `gpt-` 접두사** — 추상 클래스·중립 이벤트 모델 없음 | 향후 Gemini Live 등 다른 WS 음성 모델 대비(사용자). Gemini 프로토콜을 모르는 채 중립 모델을 설계하면 틀릴 확률이 높음 | OpenAI 전용 단일 파일(나중에 갈라내기) / 범용 Realtime 클라이언트·어댑터 | 〃 |
| **미리듣기 타임아웃은 15초 + 0.03초 × 문장 길이** | 생성이 실시간의 약 9배라 2000자(약 500초 음성)는 15초 고정으로 항상 502 (Codex P2). 연결·응답 시작 대기 최대 5초에 여유 | 고정 15초 / 허용 길이 축소 | 〃 |
| **다운링크 프록시는 v4 사본 `AudioProxyTrack`** (v3 사본 → v4, `abf7a21`) | 재생 제어 API 는 v4 사본에만 있고 v3 는 바이트 고정. dev 실측에서 v3 사본으로 `clear()` 호출 → AttributeError 로 발화 처리·종료말·멈춤 전부 중단. v4 사본은 v3 의 상위 집합(200ms 프리버퍼·좀비 가드·PROXY-DOWN 태그) | v3 사본 유지 + 재생 제어 우회 / v3 파일 수정(불가침 위반) | [[AI음성 - 일시정지·멈춤 재생 제어 구현]] |
| **오디오 탭 스위치는 플래그 파일 내용(on/off)**, 경로는 `/data/logs` 마운트 아래 | 파일 삭제 없이 켜고 끄기(사용자 요청), 컨테이너라 `/tmp` 는 호스트에서 안 보임. 세션 생성 시점 평가라 재시작 불필요 | 본체 원안(`touch`/`rm` 존재 스위치, `/tmp`) / HTTP 토글 엔드포인트(무인증 서비스에 진단 스위치 노출) / `.env`(컨테이너 재생성 필요) | [[AI음성 - 오디오 탭 복원과 업링크 무변경 확인]] |

## 게이트·음질

| 결정 | 이유 | 기각한 대안 | 근거 |
|---|---|---|---|
| **업링크 게이트 복원 후 원본(v2.6.7) 임계값에서 재출발** | 임의 상향(1.2초 2건)이 끼어들기 오탐은 못 막고 짧은 대답만 잃음 | 상향값 유지 | [[AI음성 - 게이트 원본 복귀와 임계값 재조정]] |
| **침묵 종료 400ms** | 원본 60ms 는 어절 사이 쉼마다 발화를 조각내 min_utterance 관문에서 통째 소실 (로그 실증) | 원본 60ms | 〃 |
| **실발화 기준 RMS 400 · 누적 600ms** | 600/900 은 실발화 밀도 79% 정상 질문을 폐기 (dev 로그 실측). RMS 600 재상향도 재현 후 400 으로 최종 | RMS 600 · 900ms | 〃 |
| **barge-in 트리거를 실발화(speech) 기준으로** | 소리(voice) 기준은 잡음이 1.2초 시계를 채워 조기 발동. force_flush 는 관문 적용 정산으로 전환 | voice 누적 기준 | 〃 (`0dfbbbb`) |
| **패스스루 해제를 실발화 부재 기준으로** | 무음(RMS≤50) 기준은 약한 배경음만으로 패스스루 고착 → 게이트 무력화·응답 지연 | 무음 기준 유지 | 〃 (`048c9df`) |
| **P1-1(barge-in 실패 시 누적 유지) 되돌림, P1-2(드레인 4배속) 유지** | 원본 정산(폐기·리셋) 동작 복귀 — 누적 유지가 오히려 오탐 재료가 됨 | P1-1 유지 | 〃 |
| **GA 음성 cedar 복원** | v4 전면 롤백에 쓸려나간 유실분 — 게이트 복원이 audio_tracks 범위만 되돌려 marin 으로 남아 있었음 | marin 유지 | 〃 (`afb7372`) |
| **지터 백로그 적응형 배속 페이싱 — 구현 후 되돌림** | 목표였던 **폐기 감소를 달성하지 못한다**. 폐기는 뭉치 하나가 상한(48프레임)을 넘을 때 생기는데 그때 버퍼는 이미 비어 있어 빨리 빼내도 자리가 안 생긴다. 대가로 버퍼가 지터를 못 흡수해 언더플로 침묵만 는다 | 수위>prebuffer 시 4배속 (`7550cbb` → `140ca38` revert) | 시뮬 1200ms 지터: 폐기 **600건으로 배속 유무 동일**. 700ms: 침묵 11→**508개**/60초. 얻는 건 체류 지연 260ms→0 뿐 — 모바일 실사용 체감 없음 |
| **상시 지연 감소는 시간축 신축(NetEq/WSOLA) 없이는 불가** | 페이싱은 지터를 폐기↔침묵 사이에서 옮길 뿐 없애지 못한다. 게이트 자체 지연(+1.5~2.8초)에 비하면 버퍼 체류분은 부차적이라 실익도 작다 | 리베이스 제거·무한 배속도 같은 이유로 기각 | 위와 동일 (`1db9789` 코드 주석) |
| **발화 조각남 우려는 오진 — 침묵은 100ms마다 1프레임** | 언더플로 침묵은 20ms 간격이 아니라 **타임아웃(100ms)마다 1개**씩 나온다. 525ms 공백은 26프레임이 아니라 6프레임이라 게이트 임계(연속 20)에 못 닿는다. 한때 "발화 전멸"로 판단했으나 시뮬레이터 입력 오류였다 | 26프레임 가정에 근거한 전멸 판정 (**철회**) | 정정 시뮬: 연속 침묵 최대 **7 < 20**, 게이트 조각남 없음 |
| **업링크 VAD 프레임 마스킹(Silero, 붙잡기 없음) 기각** (2026-09-04) | 오프라인 PoC 3회(정답: 발화 뭉치별 Whisper 전사 CER). 최선 변형(문턱 0.3·선행 지연 500ms)도 보존 79%/57%, 잡음 차단 63%/72%. 손실은 발화 첫 음절 — Silero 시작 감지 지연 40~600ms 라 인과 판정으로 못 피하고, 지연선을 늘리면 잡음 차단이 떨어짐 | 게이트 교체 / 게이트 안에 VAD 삽입 / GTCRN 동시 도입 / 응답 중 확인 지연 / 500ms 변형을 게이트 대안으로 구현(기준 미달이라 미채택) | [[AI음성 - 탭 녹음 분석과 업링크 VAD 마스킹 PoC 기각]] |
| **음성 판정 실험의 정답은 음량도 Whisper 단어 타임스탬프도 아니다** — 뭉치별 전사 대조로 잰다 | RMS>400 은 충격음을 발화로 셈(게이트 통과 15건 중 4건이 유성음 0%). Whisper 는 긴 무음에 환각을 내고 첫 단어 시작을 0.1~0.7초 이르게 찍음. sherpa `is_speech` 는 기본 래치(`min_speech_duration` 0.25)로 첫 8창이 무조건 False | 프레임 단위 정답 지표 | 〃 |
| **세션 자동 종료의 실제 서버 경로는 aioice 동의 검사 30초 → `failed`** — `disconnected` 30초 유예는 aiortc 가 그 상태를 만들지 않아 죽은 경로 | 탭 녹음 2건 모두 브라우저 원음이 말하는 도중 끊기고 33초 뒤 close. 프론트 JS 종료 경로는 전부 release 를 보내므로 JS 실행 없이 패킷이 멈춘 것 | 프론트 종료 로직 원인설 | 〃 |

## 보류·대기 중

| 안건 | 상태 | 근거 |
|---|---|---|
| 화자 관문 Phase 1 | **롤백** — 코드는 `backup/speaker-gate-phase1` 보존, 도입 여부 회의 결정 대기 | [[AI음성 - 화자 관문 롤백]] |
| TSE(겹침 제거) Phase A PoC | **실행 보류** — 계획만 확정 (`docs/superpowers/plans/2026-08-18-tse-phase-a-poc.md`) | [[AI음성 - TSE 도입 계획 수립]] |
| 방출 경계 전사 왜곡 | 관찰 중 — 드레인 전환부 아티팩트 추정 | [[AI음성 - 완화 게이트 최종 재측정]] |
| 대기시간 상한 | **해소(2026-08-21)** — 기획서 V1.1 이 0.1~2초로 확정. 서버 상한 3초는 더 넓을 뿐이라 무수정 | [[음성 선택과 대기시간 옵션]] |
| 폐지된 legacy 평면 스키마 경로 존치 | **해소(2026-08-21)** — 실검증 후 분기째 제거 (`04205e5`) | [[AI음성 - 1.5 GA 실검증과 legacy 경로 제거]] |
| 본체 chatbot_config 조회 경로 | 미확정 — 조회 API 신설 vs 서비스가 SQLite 직접 읽기(greeting 패턴) | [[음성 선택과 대기시간 옵션]] |
| 본체(Chatty) v4 의 RAG 0건 폴백 동기화 | 대기 — 본체 `ai_voice` 동일 블록은 아직 자체 지식 유도 상태. dev 검증 후 본체 PR | [[AI음성 - RAG 0건 폴백 미확인 안내 전환]] |
| 미리듣기 통화용 시스템 프롬프트(말투) 주입 | 대기 — 최소 설정으로 시작. 관리자가 들어 보고 통화와 말투가 다르면 추가 | [[AI음성 - 미리듣기 Realtime 1회 합성 전환]] |
| 게이트 미사용 시 잡음 오탐 횟수 | 미확정 — 오디오만으로는 2~3건 의심. dev 로그 `speech_started` 시각을 탭 A 파일과 대조해야 확정 | [[AI음성 - 탭 녹음 분석과 업링크 VAD 마스킹 PoC 기각]] |
| 프론트의 OpenAI `error` 이벤트 일괄 통화 종료·빈 ICE 상태 핸들러 | 프론트 담당 검토 대기 (`webrtc_voice_module.js:1266`, `:1101`) | 〃 |

## 관련 노트

- [[음성채팅 v4 복원]] — 진행 상황 전체
- [[AI음성 - 런타임 플로우]] — 결정이 반영된 현행 동작
