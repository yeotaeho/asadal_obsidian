---
tags:
  - 아사달
  - 프로젝트
  - AI음성
  - 음성채팅v4
created: 2026-08-06
updated: 2026-09-01
---

# 음성채팅 v4 복원

> [!summary] 한 줄 요약
> 롤백됐던 음성채팅 v4를 v3와 분리해 복원하고, 품질을 v3 동급으로 끌어올린 뒤 **모바일 실기기에서 v4 음성 대화가 동작하는 것까지 확인했다 — 과제 1번 목표 달성.** GA 즉답 경로 5커밋을 원본(main 기준) 브랜치에 이식해 main PR 준비가 끝났고, 프론트 정식 PR이 남았다. 서비스: [[AI음성]]

## 무엇을 했나

| 작업 | 상태 |
|---|---|
| ICE 진단 라우터로 모바일 실패 원인 조사 | dev 머지 (PR #728) |
| 서버 ICE 수집 5초 지연 제거 | dev 머지 (PR #736) + v4 브랜치 흡수 |
| v3 라우터 chat_id 가드 (운영 버그) | 브랜치 완료, PR 대기 |
| v4 격리 복원 Task 1~7 | 로컬 완료 |
| v4 기본 활성 전환 + 테스트 경로 정리 | 로컬 완료 |
| v3·v4 orchestrator 분리 + v4 패키지 자립 | 로컬 완료 |
| 세션 재사용 경합 수정 (세션 generation) | 로컬 완료 |
| 공용 자산 재사용 감사 + 죽은 사본 제거 | 로컬 완료 |
| dev 머지 (PR #760) | 머지 완료 |
| 브라우저 이벤트 중계 복원 | dev 배포 완료 |
| v4 무음 원인 규명 + GA 세션 설정 전달 수정 | dev 배포 완료 · 실측 검증 |
| GA 세션 평면 `session.update` 가드 | dev 배포 완료 · 실측 검증 |
| tool 제어 이벤트 중계 필터 | dev 배포 완료 |
| OpenAI 자동 응답 이중 생성 차단 | dev 배포 완료 · **말풍선 1개 확인** |
| 필터 임계값 세션 주입 (과제 2번 기반) | dev 배포 완료 |
| 응답 큐 경합 수정 (드롭·우회) | dev 배포 완료 |
| 로거 핀 제거 4건 + 업/다운링크 태그 | dev 배포 완료 · ICE 로그 확보 |
| 모바일 업링크 무도달 진단 | **방화벽 가설 — 과장님 확인 대기** |
| 음성 품질 A/B/C 실측 (필터 이득 0 확정) | 완료 · 재설계 근거 |
| 업링크 투명 탭 재설계 | **dev 배포 완료 · 실측 검증 (v3 동급)** |
| 모바일 ICE 원인 확정 (통제 실험 4종) | 완료 · 0-후보 오퍼 확정 |
| 프론트 ICE 수집 대기 패치 (dev 미러) + 모바일 실기기 검증 | **모바일 정상 동작 — 과제 1번 목표 달성** |
| v4 응답 지연 원인 규명 + GA 즉답 경로 복원 (Codex 4라운드 Ship) | **로컬 완료 · dev 배포 대기** |
| GA 즉답 경로 5커밋 원본(main 기준) 브랜치 이식 | **완료 · 푸시됨** (`02883f3`..`5f1672e`) |
| 프론트 미러 실사 — ICE 패치·GA 마이그레이션 반영 확인 | 완료 (2026-08-13) |
| Realtime 모델 2.1 전환 + GA 판정 접두사 통일 | **백엔드 완료 · 양 브랜치 커밋 · Codex 통과** |
| backup/voice-v4 전수 대조 — 유실 운영 수정 2건 발견·재이식 | **완료 · 양 브랜치 푸시 · Codex 4라운드 통과** |
| v4 런타임 플로우 전수 문서화 (7단계·예외 경로) | **완료 (2026-08-13) · 코드 무수정** |
| Tool-Only 휴면 분기 제거 (flush 가드 4종→3종, 39줄 삭제) | **완료 · 양 브랜치 커밋 · Codex 클린** |
| 잡담 검색 선언 지시문 완화 (지식 질문에만 tool 호출, v3·v4 공통) | **완료 · 양 브랜치 커밋 · Codex 클린 · 미푸시** |
| 런타임 문서 정정 + 파일·함수 콜체인 정리 (상속 오류 1건 포함) | **완료 (2026-08-14) · 코드 무수정** |
| 업링크 게이트 복원 (업무 지시, `a7e5da9` 역적용) | **완료 (2026-08-14) · Codex P1 2건 = 원본 기지 결함, 수정 보류·보고** |
| 게이트 P1 수정 (검사·정산 분리 + force 우회 + 드레인 4배속) | **완료 (2026-08-14) · Codex 재리뷰 클린** |
| 게이트 복원 dev 배포(PR #828) + A/B/C 재측정 | **완료 (2026-08-14) · barge-in +1.3초로 개선, 조각 소실 구조는 잔존 — 임계값 방향 결정 대기** |
| RAG 구조 조사 — 엔진 오기(FAISS→pgvector) 정정 + 품질 문제 3건 발견 | **완료 (2026-08-14) · 코드 무수정** |
| 2.1 VAD 감도 측정 (RMS 스윕 + 맥락 검증) | **완료 (2026-08-14) · 조용한 맥락 RMS 120 까지 감지, 미감지는 맥락 억제 — 기준선 해석 정정** |
| 게이트 임계값 완화 (200ms/500ms/RMS120) + silence_frames 주입 필드 | **커밋·Codex 클린 (2026-08-18) · 사전 검증 소실 0건 · 미푸시(인증 실패) — 배포 후 최종 재측정 대기** |
| 런타임 플로우·파일 함수 문서 게이트 반영 + v4 플로우 위젯 재렌더 | **완료 (2026-08-18) · 코드 무수정** |
| 완화 게이트 dev 배포(PR #829) + 최종 A/B/C 재측정 | **완료 (2026-08-18) · 발화 소실 0건 (2회 재현) · 지연 +1.0~1.8초 잔존** |
| 화자 구분 실시간 적용 검토 (ElevenLabs 불가 확정, 로컬 임베딩 관문 권장) | **완료 (2026-08-18) · 코드 무수정** |
| 화자 관문 Phase 1 구현 (게이트 4관문 · observe/enforce · sherpa-onnx) | **커밋 7개 · 189 passed · Codex 3라운드 해소 (2026-08-18) · dev 설치·배포·실측 대기** |
| 화자 관문 롤백 — 코드는 `backup/speaker-gate-phase1` 보존, 계획·데모만 유지 | **완료 (2026-08-18) · 도입 여부 회의 결정 대기** |
| TSE(겹침 제거·음질 향상) 도입 설계 + Phase A PoC 계획 | **계획 확정 (2026-08-18) · 실행 보류 — 3군 비교(원본/공개/상용) · CER 관문** |
| search_web 미등록 도구 지시 제거 (음성채팅 무응답 원인) | **완료 (2026-08-19) · Codex 클린 · v3 운영 동시 반영** |

브랜치 `fix/v4-session-generation-guard`(main 기준)가 v4 작업 전체를 담고 있다. dev 테스트는 사본 `fix/v4-session-generation-guard-dev`로 올려 머지한다. main PR은 사본이 아니라 **원본**으로 올려야 한다.

2026-08-13에 -dev 사본에만 있던 GA 즉답 경로 5커밋을 원본에 체리픽해 **두 브랜치의 v4 고유 소스가 동등해졌다** (핵심 파일 바이트 동일 확인). 남은 차이는 타 브랜치 소유분(v3 가드 테스트, ICE 진단 라우터 등 dev 머지 유입분)뿐이며, 이는 main에 들어가면 안 되는 게 맞다.

**dev 서버에 v4는 정상 등록돼 있다.** 이전에 적었던 `aiortc` 미설치 진단은 틀렸다 — 서버에 1.14.0이 이미 있었고, 실제 원인은 배포 액션이 돌지 않은 것이었다. 지금은 `110.45.147.56:9010`의 v4 라우트가 모두 응답하는 것을 실측으로 확인했다. 경위는 [[AI음성 - dev 배포와 aiortc 미설치 확인]] 참고.

그런데 라우트가 떠 있어도 **통화가 되지 않았다.** 원인은 v4 브릿지가 OpenAI에 붙는 단계에서 조용히 죽는 것이었고, 이번에 규명해 고쳤다 — [[AI음성 - v4 무음 원인 규명]] 참고.

## 무엇이 달라졌나

**Realtime 모델이 `gpt-realtime-2.1` 로 올라갔고, 모델 버전을 올려도 안 깨지는 구조가 됐다.** 기존에는 GA 여부를 고정 집합 `{gpt-realtime-2, gpt-realtime-2.0}` 으로 판정해서, 2.1 을 넣으면 집합에 없다는 이유로 **2026-05-12 에 폐지된 Legacy 경로로 조용히 떨어졌다**. 프론트는 같은 모델을 `startsWith('gpt-realtime-2')` 로 GA 라고 판정하므로 프론트만 정상이라 믿는 어긋난 상태가 된다. 판정을 프론트와 같은 접두사 규칙으로 통일해 다음 버전(2.2)에서도 같은 사고가 나지 않는다. 전환 전에 2.1 이 현재 v4 GA 세션 설정을 그대로 받는지 OpenAI 에 직접 물어 **대조군과 함께 HTTP 201** 을 확인했다. 경위는 [[AI음성 - Realtime 모델 2.1 전환과 GA 판정 통일]] 참고.

**복원 완전성이 오리지널 최종본 기준으로 검증됐다.** `origin/backup/voice-v4`(tip `318c0db`)와 파일·함수·인스턴스 속성 단위로 전수 대조해, 오리지널 마지막 운영 수정(PR #585)에서 유실된 2건 — 브라우저 제어 JSON의 runtime 전달 차단(RAG 이중 실행·메모리 누수 방지), 사용자 텍스트 FIFO 직렬화 — 을 찾아 재이식했다. Codex 리뷰 4라운드에서 워커 수명주기 경합 2건(잔여 항목 방치, 재접속 시 이전 워커의 늦은 정리)을 추가로 보강했다. 나머지 오리지널 기능(토큰 사용량 반환, 이벤트 중계, 필터 1초 완화, 좀비 스위퍼 등)은 모두 복원돼 있음을 확인했다. 경위는 [[AI음성 - backup 전수 대조와 유실 수정 재이식]] 참고.

**v4 응답 시작 경로가 오리지널과 같아졌다.** 복원 과정에서 GA 모델 LangGraph 스킵 가드(`e12eeea`)가 유실돼, 이중 응답을 자동 응답 차단(create_response=False)으로 누르면서 느린 서버 경로 — 전사 대기 + 턴 버퍼 1.5초 + Decision LLM + RAG 중복 — 만 남아 있었다. 오리지널 방향(자동 응답 복원 + v4 GA 세션 한정 LangGraph 스킵)으로 되돌렸고, Codex 리뷰에서 나온 경합 3건(자동 응답 pending 미표시, 끼어들기 판정 누락, 진행 중 tool 태스크 잔존)도 함께 잡았다. 실기 응답 속도 재측정은 dev 배포 후다.

**모바일에서 v4 음성 대화가 처음으로 동작한다** — 폰(안드로이드) 실기기 확인. 원인은 프론트가 ICE 후보 수집을 기다리지 않고 후보 0개 오퍼를 보내던 것이었고, 수집 대기 패치 하나로 해결됐다. 이 건으로 서버 코드는 무변경이다 — 서버는 표준대로 동작하고 있었다.

**v4 음성 품질이 v3 동급으로 올라왔다.** 업링크 게이트·지터버퍼를 걷어낸 재설계 후 dev 실측 — 발화 소실 **2/3건 → 0건**, 감지 지연 **+3.5초 → +0.3~0.5초**, 소음 오반응 0 유지. 잔여 격차는 이중 트랜스코딩의 +0.1~0.3초뿐이다.

**데스크톱 v4는 끝까지 동작한다.** 발화당 응답 하나, RAG 반영, 에러 토스트 없음 — 실기 확인. **모바일은 서버 코드 무죄가 확정됐다.** 폰의 UDP가 서버에 도달하지 않는 네트워크 문제(ICE checking 정지)로 좁혀졌고, 방화벽 소스 제한 가설로 과장님 확인 대기 중이다.

**v4가 dev에서 실제로 동작한다.** aiortc 재현 클라이언트로 3단계에 걸쳐 실측했다.

| 단계 | DataChannel 이벤트 | 인바운드 오디오 |
|---|---|---|
| 수정 전 | 0건 | **0프레임** |
| GA 세션 설정 전달 후 | 3건 (`error` 포함) | 1,123프레임 |
| `session.update` 가드 후 | **2건** | 1,107프레임 |

오디오가 초당 약 47프레임으로 끊김 없이 들어오고, 매 통화마다 뜨던 에러 토스트도 사라졌다. 경위는 [[AI음성 - v4 무음 원인 규명]]과 [[AI음성 - v4 배포 검증과 GA session.update 가드]] 참고.

v4 세션 생성이 **약 10초 → 약 10ms**로 단축됐다 (ICE 수집 지연 제거, 2개 연결 지점).

v4가 **기본 활성**이라 배포하면 바로 `/AI_voice/voice-chat-v4/*` 경로가 열린다. 문제가 생기면 `ENABLE_VOICE_V4=off`로 코드 배포 없이 즉시 끌 수 있다.

v3 운영 경로는 **바이트 단위 무변경**이며 스모크 테스트가 이를 고정한다. 라우터뿐 아니라 `v3/` 서비스 패키지 전체가 대상이다. 실제로 트래픽이 v4로 가려면 프론트 배포본에도 v4 폴백이 들어가야 한다.

**v4 소스에 `voice_chat.v3` 참조가 하나도 없다.** 세션이 어느 경로로 만들어졌는지에 따라 `server_webrtc`가 v3/v4 orchestrator를 골라주고, v4는 `v4/` 아래 자기 사본만 쓴다. 이제 v4를 고치려고 v3를 건드릴 일이 없다.

세션 정리에 **generation** 개념이 들어갔다. 같은 `session_id`가 재사용될 때 이전 세대의 늦은 정리가 새 세션을 부수던 문제를 막는다. 재현해보니 리뷰가 지목한 "예약된 콜백이 늦게 실행됨"은 일어나지 않고, 실제 창은 `close()`가 **진행 도중** 새 세대를 가로지르는 것이었다 — 그래서 진입 검사만으로는 부족하고, `close()`가 첫 `await` 전에 자기 세대 자원을 전부 떼어내도록 구조를 바꿨다. 경위는 [[AI음성 - 세션 재사용 경합 수정]] 참고.

공용 자산은 제대로 재사용하고 있다. v4가 import하는 프로젝트 모듈 **13개 전부**가 `main`에 이미 있던 것이다 — RAG 검색·챗봇 ID 조회·프롬프트 정책·세션 스키마 검증·상수 6개. 모델명·타임아웃 하드코딩 **0건**, 새 환경변수 **2개**뿐이다. 감사에서 나온 유일한 재발명은 `v4/rtp_stats_utils.py` 423줄이었고 삭제했다. 경위는 [[AI음성 - 공용 자산 재사용 감사]] 참고.

## 프론트 현황 (2026-08-13 실사)

로컬 미러(`AI_pro/voice/js/`)를 직접 읽어 확인했다. 미러는 git 저장소가 아니라 커밋 이력이 없다.

**ICE 수집 대기 패치는 반영돼 있다.** `webrtc_voice_module.js:307-333` — `setLocalDescription` 후 `icegatheringstatechange`로 수집 완료를 기다렸다가(폴백 상한 **2초** 하드코딩) 후보가 포함된 `pc.localDescription.sdp`를 POST한다. STUN은 구글 단일, TURN 없음.

**GA 마이그레이션은 2026-08-13 에 2.1 전환까지 끝났다.** 아래는 수정 후 상태다.

| 항목 | 상태 |
|---|---|
| 기본 모델 | **`gpt-realtime-2.1`** — `voice_runtime.js:3` |
| dialect 분기 | `gpt-realtime-2*` → ga, 그 외 legacy — `:189-196` (백엔드와 같은 접두사 규칙) |
| session.update | GA면 전송 생략 (flat 포맷 거부 대응) — `:1440-1445` |
| GA 런타임 에러 | **자동 폴백 제거** — 오류를 그대로 보고 |
| 모델 정규화 | `gpt-realtime` → **`gpt-realtime-2.1`** (3파일 동시 수정) |
| 이벤트 타입 | GA/legacy 양쪽 이름 모두 처리 (브리지 포함) |

`node --check` 4파일 통과. **아직 업로드 전이다** — 파일질라로 올려야 반영된다.

프론트에는 OpenAI 직접 호출이 없어서 (SDP는 전부 자체 백엔드 경유) 제거할 베타 헤더 자체가 없다 — 베타 헤더는 백엔드 관심사(`ai_voice/constants.py`).

잔여 이슈 2건.

- `webrtc_voice_widget.js:100` — 구형 독립 위젯에 `gpt-4o-realtime-preview-2024-12-17`(2026-05-07 폐지) 하드코딩. **미러 안에 참조가 없어** 사용 여부 불확실. 안 쓰이면 삭제, 쓰이면 이미 깨져 있다
- `?v=` 캐시 파라미터 전무 — 동적 로드부(`voice_webrtc_handler.js:330`)에도 없어 **파일을 고쳐도 브라우저 캐시로 구버전이 실행될 수 있다.** 이번 업로드에도 그대로 해당된다

모델 정규화가 **3파일에 중복**돼 있는 구조는 그대로다(`webrtc_voice_module.js`, `voice_input_plugin.js`, `modes/voice_webrtc_handler.js`). 다음 모델 변경 때도 3곳을 함께 고쳐야 한다.

## 왜

담당 과제 1번(AI음성 모바일 오류 수정)의 실체가 "v3 버그 수정"이 아니라 **롤백된 v4를 모바일에서 동작하게 부활**시키는 것이었다.

조사 결과 모바일 ICE/NAT(TURN 부재) 가설은 4G 실기기 테스트로 기각됐고, 진짜 병목은 서버 aiortc의 **ICE 수집 5초 고정 지연**(요청 처리 프로세스가 그동안 통째로 묶임 → 버튼 무반응)으로 실측 확인됐다. 과거 롤백의 재발을 막기 위해 v3 불가침·성공 경로 warning 금지를 전역 제약으로 강제하며 복원했다. 경위는 [[v4 복원 작업 현황]], [[운영에서만 터지는 문제들]] 참고.

그런데 복원 후 재검토에서 그 제약이 실제로는 지켜지지 않고 있었다는 게 드러났다. v4 라우터가 자체 orchestrator를 갖고도 공용 `server_webrtc`를 거쳐 v3 것을 타고 있어서, v4의 RAG 결함을 고치려면 v3 파일을 고쳐야 했다. 2026-07-13 롤백과 같은 실수가 한 층 아래에서 반복된 셈이라 세션별 분기로 정리했다.

> [!warning] 실행 구조 정정 (2026-08-13 서버 실측)
> 여러 노트가 "gunicorn 워커 4개"를 전제로 쓰여 있었는데 **근거가 없는 숫자**였다.
>
> | 항목 | 실제 |
> |---|---|
> | dev Chat App(9010) | **uvicorn 단독 실행** (`kss_app.service`). 워커 개념 없음, 프로세스 1개 |
> | 이 서버의 다른 gunicorn | 전부 **`--workers 2`** (명령줄 지정) |
> | 운영 Chat App 워커 수 | **미확인** |
> | `APP_WORKERS = 4` | `config.example.py` 기본값일 뿐 구동에 안 쓰임 |
>
> `gunicorn.conf.py` 는 `worker = ...` 로 **단수**라 gunicorn 이 인식하지 못한다(설정 이름은 `workers`). 지금은 명령줄 인자가 우선해 무해하지만 그 인자를 빼면 워커 1개가 된다 — **그대로 두기로 결정.**
>
> 실무상 함의 두 가지. **①** 이벤트 루프 불일치 수정(`_get_loop`)은 dev 에서 재현·검증이 불가능하다 — 운영 배포가 첫 검증이다. **②** v4 동시성 부하 측정을 dev 에서 해도 운영에 그대로 적용할 수 없다. 상세는 [[gunicorn 멀티워커 구조]].

## 어디

| 구분 | 내용 |
|---|---|
| 브랜치 | `fix/v4-session-generation-guard` — v4 작업 전체 (main 기준 `63ac694`..`05481dc` 28커밋) |
| 브랜치 | `feat/v4-isolated-restore` — 위에 통째로 포함됨. 버려도 됨 |
| 브랜치 | `fix/v3-router-chat-id-guard-main` (`36ec060`..`1d6d54d` 2커밋) |
| dev 머지 | PR #728 (ICE 진단), PR #736 (ICE 지연 수정) |
| 핵심 파일 | `ai_voice/services/voice_chat/server_webrtc.py` (세션별 orchestrator 분기, 세션 generation) |
| 핵심 파일 | `ai_voice/services/voice_chat/v4/` (5개 파일 — 죽은 사본 5개 제거) |
| 핵심 파일 | `ai_voice/infrastructure/api/router/voice_chat_v4_router.py` |
| 핵심 파일 | `ai_voice/voice_router.py` (게이트), `ai_voice/voice_tests/` (10파일 75건) |

두 브랜치가 `conftest.py`를 함께 건드려 dev 머지 때 add/add 충돌이 한 번 난다. 합집합으로 풀면 된다.

generation 가드는 v4 브랜치 위에 쌓았다. 지적 대상인 `_orchestrator_for`와 `voice_tests/` 자산이 main에 없어 main 기준으로는 재현도 수정도 불가능하다.

## 배포 검증 절차

dev 머지(`app restart`) 후 v4가 실제로 올라왔는지 확인한다.

```bash
curl -k https://dev.chatbaram.com:9000/openapi.json | grep -c "voice-chat-v4"
```

v4가 기본 활성이므로 **0보다 커야 정상**이다. 별도의 `ENABLE_VOICE_V4=on` 설정 단계는 필요 없다.

## 추후 작업

- [x] dev 배포 후 재현 클라이언트로 오디오 프레임 확인 — **0프레임 → 1,107프레임**, error 이벤트 소멸
- [x] 데스크톱 v4 검증 — 말풍선 발화당 1개, RAG 반영 답변, 에러 토스트 소멸
- [x] 음성 품질 개선 — 업링크 투명 탭 재설계로 **v3 동급** 실측 확인
- [x] 모바일 무반응 원인 확정 — 프론트 0-후보 오퍼 × 외부발 신규 인바운드 UDP 드롭 — [[AI음성 - 모바일 ICE 원인 확정]]
- [x] **모바일 실기기 검증 — 정상 동작, 과제 1번 목표 달성** — [[AI음성 - 모바일 정상 동작 확인]]
- [x] Realtime 모델 2.1 전환 **백엔드** — GA 판정 접두사 통일 + 죽은 기본값 제거 (`41fe01b` / `0b08c91`, dev 머지 완료)
- [x] Realtime 모델 2.1 전환 **프론트** — 4파일 수정 + 업로드 완료. 죽은 폴백 제거 포함
- [x] **2.1 dev 실통화 확인** — HTTP 200 · ICE completed 0.1초 · 이벤트 2건 · error 0건 · 오디오 871프레임(약 46 f/s). 4일차 기준선과 동일
- [x] Tool-Only 휴면 분기 제거 — 세팅처 0건 확인, v2 opt-in 계약째 삭제 (`0ef439b` / `3e43324`, **미푸시**) — [[AI음성 - Tool-Only 휴면 분기 제거]]
- [x] **게이트 복원 후속 — Codex P1 2건 수정 완료** — 검사·정산 분리(`_gate_passes`) + force_flush 관문 우회 + 드레인 4배속 (`cadf185`, `a9f821f`, 재리뷰 클린) — [[AI음성 - 게이트 P1 수정]]
- [x] **게이트 복원 dev 배포 + A/B/C 재측정** — PR #828 배포 후 2회 실측. **barge-in 지연 +3.5초 → +1.3초** 개선 확인, 조각 소실 구조 잔존 — [[AI음성 - 게이트 복원 A B C 재측정]]
- [x] **VAD 동작 재측정 — 2.1 감도 확보** — 직행 감지 +0.17~0.31초, 조용한 맥락 **RMS 120 까지 감지**, 큰 소리·응답 직후 저음량 억제(맥락 함수). 게이트 `speech_rms_threshold=300` 이 VAD 보다 보수적임이 확정. 끼어들기 처리 측정은 잔여 — [[AI음성 - VAD 감도 측정]]
- [x] **게이트 임계값 방향 결정 — 완화로 확정·구현** — 기본값 4종 완화(200ms/500ms/RMS120/120ms) + `silence_frames_to_end` 주입 필드 (`336dcdc`, Codex 클린) — [[AI음성 - 게이트 임계값 완화]]
- [x] 게이트 복원 반영 문서 갱신 — [[AI음성 - 런타임 플로우]] 2단계·[[AI음성 - 파일·함수 레퍼런스]] 를 `336dcdc` 기준으로 전면 반영 + 플로우 위젯 7장 재렌더 — [[AI음성 - 런타임 문서 게이트 반영과 플로우 위젯 재렌더]]
- [x] **완화 배포(PR #829) + 최종 A/B/C 재측정 — 발화 소실 0건 (2회 재현)** — "네." +1.0초 도달, 꼬리 문장 복구. 잔존 비용: 지연 +1.0~1.8초, 방출 경계 경미한 전사 왜곡 — [[AI음성 - 완화 게이트 최종 재측정]]
- [x] 화자 관문 Phase 1 구현 → **롤백 (회의 결정 대기)** — 코드는 `backup/speaker-gate-phase1`(커밋 7개) 보존, 스펙·계획 docs/superpowers, 데모 Downloads. 실사용 테스트에서 교대 발화 정상 구분, 감정 최저 0.4 → 등록 강화 필요, 겹침은 별도 과제(TSE) — [[AI음성 - 화자 관문 롤백]]
- [ ] **화자 관문 도입 여부 회의** — 승인 시: `backup/speaker-gate-phase1` 머지 → 등록 강화(2~3발화 평균) 반영 → dev 설치(sherpa-onnx + 모델 + env) → 관찰 실측 → 임계값 확정 → enforce. 판단 재료는 [[AI음성 - 화자 관문 롤백]] 참고
- [ ] **원본 브랜치로 백엔드 main PR** — 게이트 4커밋(`4fd394a`..`336dcdc`)을 원본(main 기준)에 이식 후 PR
- [ ] 방출 경계 전사 왜곡 관찰 — "세 시에" → "세례/세" (2회). 드레인 전환부 아티팩트 추정, 실사용 체감 확인
- [ ] **브라우저 실기 확인** — 재현 클라이언트는 프론트를 거치지 않는다. 업로드한 JS 가 실제로 2.1 을 보내는지 시크릿 창/강력 새로고침으로 확인 (`?v=` 부재로 캐시 잔존 위험)
- [ ] **GA 즉답 경로 응답 속도 재측정** (`e1e9896`..`ca73d10`)
- [x] **Gitea `Ji-Hwan/frontend` 정식 PR** — ICE 수집 대기 패치 + GA 분기 + 2.1 전환. 미러 직반영은 테스트용이라 저장소에 올려야 유실되지 않는다
- [x] JS 참조부 `?v=` 캐시 파라미터 증가 — 8/13 기준 여전히 0건, 동적 로드부에도 없음
- 인프라 외부발 신규 인바운드 UDP 허용 — **하지 않기로 결정** (프론트 패치로 해결됨). TURN 도입도 하지 않기로 결정
- [ ] 프론트 전달 (부차) — `speech_started` 말풍선 자리 잡기 + `transcription.delta` 실시간 자막, 잠금 실패가 "마이크 권한 필요"로 표기되는 버그, http→https 리다이렉트 부재
- [ ] 과제 2번 — 시간 옵션 0.1~3초: 백엔드 배관 완료(`silence_frames_to_end` 포함 7필드 주입 가능). **게이트 부활로 전부 실동작 파라미터** — UI 노출 남음. 2.1 VAD 기준선(감지 +0.2~0.3초) 확보됨
- [ ] 저SNR 잔여 격차 관찰 — 이중 트랜스코딩 +0.1~0.3초. **저음량일수록 전사 왜곡**("연세고"→"연습용") 실측 — 저레벨 양자화 열화
- [ ] `deploy-dev.yml` — 의존성 설치 단계 없음. 플래그 오타(`app start`)도 조용히 통과. **로컬 `agent_env` 의 fastapi 도 핀(0.115.9)과 어긋나 있다**(현재 0.141.1) — 라우트 열거 테스트 1건이 이 때문에 실패한다
- [ ] **Beta API 폐지 대응** — 서버 기본 모델값·프론트 폴백·모델 정규화는 **완료**. 잔여: `webrtc_voice_widget.js:100` preview 모델 하드코딩, 정규화 3파일 중복 구조
- [ ] v4 세션 응답에 `session_id` 부재 — 프론트 `getReleaseEndpoint()` 가 빈 문자열을 반환해 **release 를 호출하지 못한다.** 좀비 스위퍼에만 의존하는 상태
- [x] **모바일 마이크 자동 비활성화 원인 확정·수정** — 프론트 `pagehide` 즉시 release 가 원인. 앱 전환·화면 잠금만으로 서버가 세션을 무조건 정리해 마이크까지 꺼졌다. 유예 종료(60초) + 복귀 시 취소로 수정 — [[AI음성 - 모바일 세션 종료와 프리버퍼 로그 규명]]
- [x] **프리버퍼 경고 78건 규명** — 성공 0 / 타임아웃 78, 전 세션 100% 구조적 타임아웃. 대기 1.0→0.2초 단축 + `buffered=0` 을 debug 로 (`e5da884`)
- [ ] **AI 목소리 끊김 실측** — 다운링크 언더플로는 이틀 2건(count=1)뿐이라 서버 구간 무혐의. 폐기 카운터가 10분 주기 상태 로그에만 실려 미측정 — 주기 단축 또는 진단 엔드포인트 필요
- [ ] `search-web` 엔드포인트 v3·v4 양쪽 부재(404)
- [ ] `_output_relay` 죽은 코드 제거, v4 라우터 로거 핀
- [ ] Codex 후속 — `create_answer` 세션별 Lock, `VoiceChatGraphRuntime` generation 인지
- [ ] 과제 3번 — 음성 종류 10여종, 과제 4번 — 전화상담 (TURN 미도입 전제로 설계. **멀티채널 설계 시 겹침 문제 원천 회피 가능** — 화자 구분 검토의 교훈)
- [ ] v4 동시성 부하 측정, (선택) v3 자체 수정, (선택·낮음) `safe_int` OverflowError
- [ ] **TSE Phase A PoC 실행 (보류 중)** — 계획 확정 상태. 시작 시 `docs/superpowers/plans/2026-08-18-tse-phase-a-poc.md` Task 1 부터. 전제(화자 관문 인프라)와 함께 회의 상정 권장 — [[AI음성 - TSE 도입 계획 수립]]
- [ ] 잡담·일반상식 질문에 RAG 호출 자체를 건너뛰는 지시문 추가 완화 — search_knowledge_base 왕복 5초 지연의 근본 원인. [[AI음성 - search_web 미등록 도구 지시 제거]] 후속
- [ ] dev 로그 `elapsed_ms` 로 tool call 실제 소요 계측, 취소된 tool call 이 이후 턴에 남기는 영향 실측
- [x] **지터 백로그 적응형 배속 페이싱** — 수위 > prebuffer 면 4배속으로 빚 상환, 래칫·발화 앞부분 잘림 해소. 업링크 전용(다운링크 1배속 유지), `fix/v4-jitter-backlog-catchup` `7550cbb`, 244 passed·Codex 클린 — [[AI음성 - 지터 백로그 적응형 배속 페이싱]]
- [ ] 지터 배속 dev 배포 + 모바일 실측 — 만수위(47/48)·dropped_frames 감소 확인
- [ ] 구멍 보존 pts 검토 — aiortc 송신부가 pts 갭을 RTP 갭으로 넘기는지 실측 후 결정

## 일일 기록

- [[AI음성 - 지터 백로그 적응형 배속 페이싱]]

- [[AI음성 - 오디오 탭 실측과 문제 종합]]

- [[AI음성 - 모바일 세션 종료와 프리버퍼 로그 규명]]

- [[AI음성 - 게이트 원본 복귀와 임계값 재조정]]

- [[AI음성 - search_web 미등록 도구 지시 제거]]

- [[AI음성 - TSE 도입 계획 수립]]

- [[AI음성 - 화자 관문 롤백]]

- [[AI음성 - 화자 관문 Phase 1 구현]]

- [[AI음성 - 완화 게이트 최종 재측정]]
- [[AI음성 - 화자 구분 실시간 적용 검토]]

- [[AI음성 - 런타임 문서 게이트 반영과 플로우 위젯 재렌더]]

- [[AI음성 - 게이트 임계값 완화]]

- [[AI음성 - VAD 감도 측정]]

- [[AI음성 - RAG 구조 조사와 문서화]]

- [[AI음성 - 게이트 복원 A B C 재측정]]

- [[AI음성 - 게이트 P1 수정]]

- [[AI음성 - 업링크 게이트 복원]]

- [[AI음성 - 런타임 문서 정정과 파일·함수 콜체인 정리]]

- [[AI음성 - 잡담 검색 선언 지시문 완화]]

- [[AI음성 - Tool-Only 휴면 분기 제거]]

- [[AI음성 - v4 런타임 플로우 전수 문서화]]

- [[AI음성 - backup 전수 대조와 유실 수정 재이식]]

- [[AI음성 - Realtime 모델 2.1 전환과 GA 판정 통일]]

- [[AI음성 - 원본 브랜치 동등화와 프론트 기록 최신화]]

- [[AI음성 - v4 응답 지연 원인 규명과 GA 즉답 경로 복원]]

- [[AI음성 - 모바일 정상 동작 확인]]

- [[AI음성 - 모바일 ICE 원인 확정]]

- [[AI음성 - 업링크 투명 탭 재설계]]
- [[AI음성 - 음성 품질 A B C 실측]]

- [[AI음성 - 모바일 업링크 무도달 진단]]
- [[AI음성 - 오디오 로거 핀 제거와 방향 태그]]
- [[AI음성 - 필터 임계값 주입과 응답 큐 경합 수정]]
- [[AI음성 - OpenAI 자동 응답 이중 생성 차단]]
- [[AI음성 - tool 제어 이벤트 필터]]

- [[AI음성 - v4 배포 검증과 GA session.update 가드]]

- [[AI음성 - v4 무음 원인 규명]]
- [[AI음성 - 브라우저 이벤트 중계 복원]]

- [[AI음성 - dev 배포와 aiortc 미설치 확인]]
- [[AI음성 - 공용 자산 재사용 감사]]
- [[AI음성 - 세션 재사용 경합 수정]]
- [[AI음성 - v3·v4 orchestrator 분리]]
- [[AI음성 - v3 가드 브랜치 정리]]
- [[AI음성 - v4 기본 활성 전환과 테스트 경로 정리]]
- [[AI음성 - v4 격리 복원 (Task 1~7)]]
- [[AI음성 - v3 라우터 chat_id 가드]]
- [[AI음성 - 서버 ICE 수집 5초 지연 제거]]
- [[AI음성 - ICE 진단 라우터]]
