---
tags: [아사달, 프로젝트, 챗봇]
created: 2026-08-28
updated: 2026-08-28
---

# 챗봇

> [!summary] 한 줄 요약
> AI프로 챗봇 본체(대화·모델 선택·RAG)를 담당한다. 현재는 계정이 고를 수 있는 LLM 목록에 신규 모델을 붙이는 작업을 이어가고 있다.

## 서비스 개요

멀티테넌트 학습형 챗봇 플랫폼의 대화 본체다. 백엔드는 `Chatty_Project` 루트의 Chat App(`app.py`)이고, 계정별 `account_setting.yaml` 의 `model_name` 값으로 어떤 LLM 을 쓸지 결정한다.

모델 선택 로직은 `utils/whoami.py` 의 `get_model_by_name()` **단일 지점**에 모여 있다. OpenAI·Anthropic·Google·xAI·Mistral·Clova·Qwen·Kimi·Upstage·EXAONE·Doubao·GLM·Llama 와 RunPod SLLM 까지 전부 이 elif 체인이 분기한다. 별도의 모델 화이트리스트나 토큰 단가표는 레포에 없다.

## 기능 현황

| 기능 (과제) | 상태 | 노트 |
|---|---|---|
| Gemini 3.6 Flash 추가 (`feat/gemini-3-6-flash`) | 커밋 완료, push·PR 대기 | [[챗봇 - Gemini 3.6 Flash 모델 추가]] |
| GLM-5.3 추가 (`feat/glm-5-3`) | 커밋 완료, push·PR 대기 | [[챗봇 - GLM-5.3 모델 추가]] |

두 건 모두 1커밋짜리 모델 등록이라 별도 기능 노트를 만들지 않고 일일 노트로 남겼다.

## 모델 추가 시 알아둘 것

`get_model_by_name()` 에는 provider 별 **접두사 폴백**이 이미 깔려 있다 (`startswith("gemini-")`, `startswith("glm-")`, `startswith("claude-")` 등). 그래서 정식 모델 ID 를 그대로 쓰는 경우 **코드를 고치지 않아도 대개 동작한다**. 명시 분기를 추가하는 실익은 두 가지다.

- 짧은 별칭 제공 (`gemini-3.6`, `glm5.3`)
- 기존 분기와의 스타일 일관성, 그리고 어떤 모델이 공식 지원인지 코드에서 읽히는 것

바꾸지 않은 것도 있다. **bare 별칭의 기본값**은 옛 버전에 묶여 있다 — `gemini` / `google` → `gemini-2.5-flash`, `glm` / `zhipu` → `glm-5.2`. 승격 여부는 별건이라 손대지 않았다.

토큰 단가는 구글 시트에서 관리한다. 레포의 `utils/api_billing.py` 는 이미지 생성(Replicate·DALL·E·Recraft) 전용이라 텍스트 LLM 과는 무관하다.

## 추후 작업

- [ ] `feat/gemini-3-6-flash`, `feat/glm-5-3` push → dev PR(`app restart`) → main PR
- [ ] 실환경에서 두 모델 실제 응답 확인 (로컬은 의존성·`config.py` 부재로 런타임 검증 불가)
- [ ] 단가표 25행 한글명 `제미나이 3.5 플래시` → `제미나이 3.6 플래시` 수정
- [ ] (선택) bare 별칭 `gemini` / `glm` 기본값을 상위 버전으로 올릴지 결정
- [ ] (선택) `whoami.py` 테스트 부재 — 모델 분기 회귀 테스트를 둘지 검토

## 상세 문서

아직 없다. 이 서비스 코드를 본격적으로 다루게 되면 폴더·파일 역할 지도, 런타임 플로우, 파일·함수 레퍼런스, 결정 기록을 `spoke/` 에 작성한다.
